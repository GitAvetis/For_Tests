# ОПИСАНИЕ БОЛЬШЕ ДЛЯ ФРОНТА (ниже общие сведения по файлам)
### Пример игрового цикла

```csharp
using ProjectTeam01.domain.Session;
using ProjectTeam01.presentation;
using ProjectTeam01.presentation.ViewModels;

class GameFrontend
{
    private GameController? _controller;
    
    public void StartGame(bool loadFromSave = false)
    {
        GameSession gameSession;
        
        if (loadFromSave && File.Exists("game_save.json"))
        {
            // Загружаем сохраненную игру - уровень восстанавливается автоматически из сохранения
            gameSession = GameDataService.LoadGame("game_save.json");
        }
        else
        {
            // Создаем новую игру с первого уровня
            gameSession = GameInitializer.CreateNewGame(levelNumber: 1);
        }
        
        _controller = new GameController(gameSession);
        
        // Игровой цикл
        while (_controller.Running)
        {
            // Получить состояние
            var viewModel = _controller.GetGameStateViewModel();
            
            // Отрисовка
            
            // Обработка ввода
            if (HasInput())//не существует
            {
                var key = ReadKey(); //не существует
                _controller.HandleInput(key); // сущетсвует в контроллере
            }
        }
        
    }
    
}
```
## Руководство для фронтенд

### API для работы с игрой

#### GameController — основной интерфейс

**Методы:**

1. **`bool HandleInput(char key)`** [presentation/GameController.cs:34]
   - Обрабатывает ввод пользователя
   - Возвращает `true` если игра продолжается, `false` если выход
   - **Автоматически сохраняет статистику** каждый ход [presentation/GameController.cs:67]

2. **`GameStateViewModel GetGameStateViewModel()`** [presentation/GameController.cs:89]
   - Возвращает полное состояние игры для отрисовки
   - Вызывайте после каждого хода для обновления экрана

3. **`GameStatistics GetStatistics()`** [presentation/GameController.cs:96]
   - Возвращает статистику игры (убитые враги, собранные сокровища и т.д.)

4. **`bool Running`** [presentation/GameController.cs:24]
   - Свойство, указывающее продолжается ли игра
   - Становится `false` при выходе или завершении игры

5. **`GameSession Session`** [presentation/GameController.cs:25]
   - Свойство для доступа к `GameSession`
   - Используется для сохранения игры (нужен доступ к данным уровня)

### ⚠️ Важно: GameStateMapper

**Фронтенд НЕ должен использовать `GameStateMapper` напрямую!**

**Почему:**
- `GameStateMapper` — это `internal static class` [presentation/Mappers/GameStateMapper.cs:25], доступен только внутри сборки `presentation`
- Контроллер уже инкапсулирует работу с маппером через метод `GetGameStateViewModel()`

**Правильный способ:**
```csharp
// ✅ ПРАВИЛЬНО: используйте GameController
var viewModel = controller.GetGameStateViewModel();

// ❌ НЕПРАВИЛЬНО: не используйте маппер напрямую
// var gameState = session.GetGameState();
// var viewModel = GameStateMapper.ToViewModel(gameState); // НЕДОСТУПНО извне!
```

**Где находится маппер:**
- Файл: `presentation/Mappers/GameStateMapper.cs`
- Используется внутри: `GameController.GetGameStateViewModel()` [presentation/GameController.cs:100-103]

**Что делает маппер:**
Преобразует внутренние объекты Domain (`GameState`) в простые структуры данных (`ViewModels`) для отображения. Подробное описание методов маппера см. в комментариях внутри файла [presentation/Mappers/GameStateMapper.cs].

### Структура GameStateViewModel

**Основные свойства:**

Определен в [presentation/ViewModels/GameStateViewModel]

#### PlayerViewModel

Определен в [presentation/ViewModels/PlayerViewModel]

**Использование:**
- Отрисовка игрока на карте по `Position`
- Отображение HP-бара: `Health / MaxHealth`
- Показ характеристик в UI
- Блокировка действий если `IsSleep == true`

#### EnemyViewModel

Определен в  [presentation/ViewModels/EnemyViewModel]

**Важно при отрисовке:**
Пример реализации отрисовки врагов можно посмотреть в [presentation/GameStateRenderer.cs:76-91]:
- Пропуск мертвых врагов: проверка `enemy.IsDead` [presentation/GameStateRenderer.cs:78]
- Пропуск невидимых призраков: проверка `enemy.IsInvisible == true` [presentation/GameStateRenderer.cs:79]
- Получение символа врага: метод `GetEnemySymbol()` [presentation/GameStateRenderer.cs:115-127]
- Отрисовка: присваивание в массив `map[pos.Y, pos.X] = enemySymbol;` [presentation/GameStateRenderer.cs:90]

**Важно:** Враг удаляется сразу после смерти (RemoveEntity), поэтому во фронтенде мертвые враги обычно не попадают. Проверка `IsDead` — дополнительная защита на случай race condition или если враг еще не удален в момент получения состояния.

**Примечание:** В текущей реализации Mimic не обрабатывается отдельно при отрисовке. Фронтенд может добавить свою логику для отрисовки Mimic как предмета, если `MimicRepresentation != MimicsRepresentation.Mimic`.

#### ItemViewModel 

Определен в [presentation/ViewModels/ItemViewModel.cs]

Содержит свойства для отображения предметов: `Position`, `Type`, и опциональные свойства в зависимости от типа предмета (`WeaponType`, `StrengthBonus`, `HealthValue`, `ElixirType`, `ScrollType`, `Price`).

**Использование:** Пример реализации отрисовки предметов можно посмотреть в [presentation/GameStateRenderer.cs:94-111]:
- Получение символа предмета: метод `GetItemSymbol()` [presentation/GameStateRenderer.cs:130-141] — возвращает символы: `Treasure` = '$', `Food` = 'F', `Elixir` = 'e', `Scroll` = '?', `Weapon` = 'W'
- Отрисовка: присваивание в массив `map[pos.Y, pos.X] = itemSymbol;` [presentation/GameStateRenderer.cs:109]
- Проверка позиции: метод `IsValidPosition()` [presentation/GameStateRenderer.cs:151-155]
- Проверка перекрытия: предметы не отрисовываются поверх игрока, врагов или выхода [presentation/GameStateRenderer.cs:100-102]

**Примечание:** Фронтенд может использовать свойства `HealthValue`, `Price` и другие для отображения подсказок при наведении или выборе предмета.

#### LevelViewModel

Определен в [presentation/ViewModels/]

**RoomViewModel:**

Определен в [presentation/ViewModels/]

**CorridorViewModel:**

Определен в [presentation/ViewModels/]

**Отрисовка уровня:** Пример реализации отрисовки можно посмотреть в [presentation/GameStateRenderer.cs]:
- Отрисовка комнат: `RenderRoom()` [presentation/GameStateRenderer.cs:158-191] — рисует стены (`#`) по периметру и пол (`.`) внутри
- Отрисовка коридоров: `RenderCorridor()` [presentation/GameStateRenderer.cs:194-210] — рисует пол коридора (`.`)
- Отрисовка дверей: `RenderDoors()` [presentation/GameStateRenderer.cs:212-225] — рисует двери (`+`)
- Отрисовка выхода и старта: в методе `Render()` [presentation/GameStateRenderer.cs:47-50, 52-55]

**Примечание:** В реальном коде используется массив `char[,] map` для отрисовки. Фронтенд может использовать любой подход (массив, Canvas API, и т.д.) в зависимости от технологии.

### Важные моменты

1. **Всегда проверяйте `IsDead` для врагов** — мертвые враги не должны отрисовываться
Важно: враг удаляется сразу после смерти (RemoveEntity), поэтому во фронтенде мертвые враги обычно не попадают. Проверка IsDead — дополнительная защита на случай race condition или если враг еще не удален в момент получения состояния.
2. **Проверяйте `IsInvisible` для Ghost** — невидимые призраки не видны на карте
3. **Mimic маскируется под предметы** — проверяйте `MimicRepresentation` перед отрисовкой
4. **Обновляйте ViewModel после каждого хода** — вызывайте `GetGameStateViewModel()` после `HandleInput()`
5. **Обрабатывайте состояние сна** — если `Player.IsSleep == true`, блокируйте действия игрока
6. **Специфичные свойства могут быть null** — проверяйте `HasValue` перед использованием (например, `item.HealthValue.HasValue`)

### Работа с инвентарем

**Как работает выбор предметов из инвентаря:**

При нажатии клавиш `h`, `j`, `k`, `e` вызываются методы `HandleWeaponSelection()`, `HandleFoodSelection()`, `HandleElixirSelection()`, `HandleScrollSelection()` [presentation/GameController.cs:103-163]. Эти методы:

1. Получают список предметов через `_session.GetPlayerWeapons()`, `GetPlayerFood()`, и т.д.
2. Выводят список в консоль через `ShowItemList()` [presentation/GameController.cs:167-175]
3. Ждут ввода номера через `GetNumericInput()` [presentation/GameController.cs:190-196] (использует `Console.ReadLine()`)
4. Создают соответствующий `PlayerAction` и возвращают его


// Пользователь нажимает 'h' (оружие)
// GameController.HandleInput('h') вызывает HandleWeaponSelection()
// В консоль выводится:
//   Оружие (0 - снять, 1-9 - экипировать):
//     1. Оружие: Sword (+5 силы)
//     2. Оружие: Bow (+3 силы)
//   Выберите: _
// Пользователь вводит номер, создается PlayerAction, передается в ProcessTurn()


---
## Использование DataLayer из фронтенда

**Важно:** В текущей реализации `GameController` **не содержит методов сохранения/загрузки полной игры**. Фронтенд должен вызывать `GameDataService` напрямую через `controller.Session` для получения данных.

**Поток вызовов для сохранения/загрузки:**

```
Frontend → GameController.Session → получение данных
  ↓
GameDataService.CreateSave() → создание GameSave
  ↓
GameDataService.SaveToFile() → сохранение в JSON

Frontend → GameDataService.LoadGame() → загрузка из JSON
  ↓
GameSession (готовая сессия)
  ↓
new GameController(gameSession) → создание контроллера
```

**Для получения таблицы лидеров:**
```csharp
// Получить топ попыток
var topAttempts = GameDataService.GetTopAttempts("scoreboard.json", count: 10);
foreach (var attempt in topAttempts)
{
    Console.WriteLine($"Сокровищ: {attempt.TreasuresCollected}, Уровень: {attempt.MaxLevelReached}");
}
```

**Важно:** Сохранение полной игры происходит автоматически:
- При переходе на новый уровень (после прохождения уровня)
- При выходе из игры (клавиша `q/Q` или `ESC`)

Фронтенд не должен вызывать сохранение вручную 

### Важные детали

1. **Формат сохранения:** JSON с форматированием (indented) для читаемости
2. **Null-значения:** Игнорируются при сериализации (`JsonIgnoreCondition.WhenWritingNull`)
3. **Восстановление состояния:** Все специфичные свойства врагов (OgreCooldown, IsInvisible и т.д.) сохраняются и восстанавливаются
4. **Статистика:** Сохраняется отдельно каждый ход для обновления во время игры
5. **Таблица лидеров:** Хранит все попытки прохождения, сортируется по количеству сокровищ

# ОБЩЕЕ ОПИСАНИЕ
## Структура Domain Layer

### 📁 Session/ — Управление игровой сессией

#### `GameSession.cs`
**Центральный агрегат** игрового процесса. Хранит текущий уровень, игрока, генераторы и статистику. Реализация разнесена по partial-файлам для читаемости.

**Основные компоненты:**
- `_currentLevel` — текущий уровень подземелья
- `_player` — игрок (Hero)
- `_levelGenerator` — генератор уровней
- `_entityGenerator` — генератор сущностей (враги, предметы)
- `_mapQuery` — адаптер для запросов к карте
- `_statistics` — статистика игры

#### `GameSession.Turn.cs`
**Обработка хода игрока.** Метод `ProcessTurn(PlayerAction)` — единая точка входа для всех действий игрока. Сначала обновляет эффекты через `Player.ActiveEffectManager.TickEffects()`. Если игрок спит (`IsHeroSleep`), только обновляет эффекты и запускает ход врагов. Иначе обрабатывает действие через `switch`: `Move` → `HandlePlayerMove()` (проверяет врага на клетке, вызывает `ProcessCombat()` если есть, иначе `MovePlayer()`), `UseItem` → `UsePlayerItem()`, `EquipWeapon` → `EquipPlayerWeapon()`, `UnequipWeapon` → `UnequipPlayerWeapon()`, `DropItem` → `DropPlayerItem()`, `Quit` → завершение. После действия проверяет выход (`IsPlayerAtExit()`) и переход на следующий уровень (`AdvanceToNextLevelInternal()`), затем запускает `ProcessEnemiesTurn()`.

#### `GameSession.Movement.cs`
**Логика перемещения.** Метод `MoveEntity(IGameObject, int, int)` проверяет `IsWalkable()` и `IsOccupied()`, перемещает сущность через `entity.MoveTo()`, обновляет позицию в `Level` через `UpdateEntityPosition()`. `MovePlayer()` вызывает `MoveEntity()` и записывает статистику, затем вызывает `CheckForInteractions()` для автоматического подбора предметов на клетке через `PickupItem()`.

#### `GameSession.Combat.cs`
**Боевая система.** Метод `ProcessCombat(Enemy)` вызывается когда игрок пытается переместиться на клетку с врагом. Обрабатывает бой: атака игрока → проверка смерти врага → атака врага → проверка смерти игрока. Методы расчета урона: `CalculatePlayerDamage()` (с учетом оружия и эффектов), `CalculateEnemyDamage()` (разные формулы для Vampire, Ogre, стандартных врагов). `CalculateTreasureValue()` рассчитывает стоимость сокровищ за убитого врага. `ApplyEnemySpecialOnHit()` применяет спецэффекты при попадании врага (Snake усыпляет, Ghost становится видимым).

#### `GameSession.Enemies.cs`
**Управление врагами.** Метод `ProcessEnemiesTurn()` обрабатывает ход всех врагов. Для каждого живого врага создается поведение через `CreateEnemyBehavior()` (фабрика по типу врага), затем вызывается `behavior.Tick(Player)`. После хода обновляется позиция врага в `Level` через `UpdateEntityPosition()`. Параметр `skipEnemy` позволяет пропустить врага, который уже участвовал в бою в этом ходу.

#### `GameSession.Inventory.cs`
**Управление инвентарем.** Методы: `PickupItem()` (добавляет в `HeroBackpack`, удаляет с уровня), `UsePlayerItem()` (использует через `Backpack.UseItem()`, записывает статистику по типам), `EquipPlayerWeapon()` (экипирует оружие, при наличии предыдущего оружия выбрасывает его на карту через `FindFreeNeighborPosition()`), `UnequipPlayerWeapon()` (снимает оружие), `DropPlayerItem()` (удаляет из рюкзака, размещает на карте). Методы получения списков: `GetPlayerWeapons()`, `GetPlayerFood()`, `GetPlayerElixirs()`, `GetPlayerScrolls()`.

#### `GameSession.Leveling.cs`
**Переход между уровнями.** Методы `AdvanceToNextLevel()` и `AdvanceToNextLevelInternal()` для перехода на следующий уровень. Проверка условий завершения игры (`IsLastLevel()`, `IsPlayerAtExit()`).

#### `GameSession.State.cs`
**Получение состояния.** Метод `GetGameState()` собирает все данные о текущем состоянии игры для передачи в маппер и дальнейшей отрисовки.

#### `GameState.cs`
**DTO состояния игры.** Простая структура данных, содержащая все необходимое для отображения: позиция игрока, здоровье, характеристики, списки врагов и предметов, геометрия уровня.

#### `GameStatistics.cs`
**Статистика игры.** Отслеживает количество убитых врагов, собранных сокровищ, пройденных уровней, использованных предметов. Используется для таблицы лидеров.

#### `GameInitializer.cs`
**Инициализация новой игры.** Фабричный метод `CreateNewGame()` создает новый уровень, размещает игрока, врагов и предметы, возвращает готовую `GameSession`.

---

### 📁 Combat/ — Боевая система

#### `BattleService.cs`
**Сервис расчета боя.** Статический метод `HitSuccess()` рассчитывает вероятность попадания на основе ловкости атакующего и цели. Используется в `GameSession.Combat.cs` при обработке боя.

#### `BattleConstants.cs`
**Константы боевой системы.** Значения для расчета попадания: `InitialHitChance`, `StandardAgility`, `AgilityFactor`. Используются в `BattleService`.

#### `CombatResult.cs`
**Результат боя.** Enum с возможными исходами: `EnemyDefeated`, `PlayerDefeated`, `NoDamage`, `BothAlive`.

---

### 📁 generation/ — Генерация уровней

#### `Level.cs`
**Представление уровня подземелья.** Хранит геометрию (комнаты, коридоры), сущности (враги, предметы), позиции старта и выхода. Методы для проверки проходимости (`IsWalkable()`), занятости клеток (`IsOccupied()`), поиска комнат/коридоров (`FindRoomAt()`, `FindCorridorAt()`).

#### `LevelGenerator.cs`
**Генератор уровней.** Создает случайную карту: генерирует комнаты, соединяет их коридорами, определяет стартовую и конечную комнаты, размещает двери.

#### `EntityGenerator.cs`
**Генератор сущностей.** Размещает на уровне игрока, врагов и предметы. Методы: `PlacePlayer()` (создает нового Hero), `PlaceExistingHero()` (перемещает существующего Hero на новый уровень), `PlaceEnemies()` (количество: `3 + levelNumber / 2` до `baseEnemyCount + 5`, размещает в нестартовых комнатах), `PlaceItems()` (количество: `10 - levelNumber / 3` до `baseItemCount + 5`, минимум 3). `CreateRandomEnemy()` создает случайного врага (равномерное распределение по типам). `CreateRandomItem()` определяет вероятности: 35% еда, 25% эликсир, 20% свиток, 20% оружие.

#### `Room.cs`
**Комната.** Прямоугольная область с координатами `TopLeft` и `BottomRight`, массивом дверей, флагами `IsStartRoom` и `IsEndRoom`.

#### `Corridor.cs`
**Коридор.** Список клеток (`Cells`) и тип (`Horizontal`, `Vertical`, `LShape`, `TShape`, `Cross`).

#### `Position.cs`
**Координаты.** Простая структура с `X` и `Y` для представления позиции на карте.

#### `LevelMapQuery.cs`
**Адаптер для запросов к карте.** Реализует интерфейс `IMapQuery` для работы с `Level` без прямой зависимости. Используется в `EnemyBehavior` для поиска путей и проверки проходимости.

#### `IMapQuery.cs`
**Интерфейс для запросов к карте.** Определяет методы `IsOccupied()`, `GetDistance()`, `FindRoomAt()`, `FindCorridorAt()` для работы с картой.

#### `DSU.cs`
**Disjoint Set Union.** Структура данных для объединения множеств. Используется в `LevelGenerator` для проверки связности комнат.

---

### 📁 Characters/ — Персонажи

#### `Сharacter.cs`
**Базовый класс персонажа.** Хранит общие характеристики: `ActualHp`, `MaxHp`, `BaseAgility`, `BaseStrength`. Базовый класс для `Hero` и `Enemy`.

#### `Hero.cs`
**Игрок.** Наследуется от `Character`. Управляет инвентарем (`Backpack`), эффектами (`ActiveEffectManager`), оружием (`WeaponManager`). Имеет флаг `IsHeroSleep` для состояния сна.

#### `Enemy.cs`
**Базовый класс врага.** Наследуется от `Character`. Хранит `EnemyType`, `HostilityLevel` (радиус активации), `IsTriggered` (флаг активации), `IsDead` (флаг смерти). Содержит `EnemyBehavior` для AI.

#### `Zombie.cs`, `Vampire.cs`, `Ghost.cs`, `Ogre.cs`, `Snake.cs`, `Mimic.cs`
**Конкретные типы врагов.** Каждый определяет свои характеристики (HP, ловкость, сила, уровень враждебности) и может иметь уникальные свойства:
- **Vampire**: `EvadedFirstAttack` — первая атака всегда промахивается
- **Ghost**: `IsInvisible` — может быть невидимым
- **Mimic**: `Representation` — может маскироваться под предмет (Food/Elixir/Scroll/Weapon)

---

### 📁 Characters/Behavior/ — AI врагов

#### `CharacterBehavior.cs`
**Базовый класс поведения.** Абстрактный класс, реализующий `ICharacterBehavior`. Содержит метод `Attack(Character target)` который использует `BattleService.HitSuccess()` для проверки попадания и наносит урон через `target.TakeDamage()`. Метод `TakeDamage(int damageValue)` делегирует урон персонажу. Используется как базовый класс для `EnemyBehavior`.

#### `EnemyBehavior.cs`
**Базовое поведение врага.** Наследуется от `CharacterBehavior`. Абстрактный класс с методом `Tick(Hero hero)`. Реализует общую логику движения: `MoveTowards()` (поиск ближайшей клетки к игроку через BFS), `MoveRandom()` (случайное движение), `TryMoveTo()` (проверка и перемещение), `DistanceToHero()` (расчет расстояния через `IMapQuery.GetDistance()`). Метод `Attack()` наследуется от `CharacterBehavior` и вызывается когда враг на расстоянии 1 от игрока. Виртуальный метод `SpecialEffectOnAttack()` переопределяется в `SnakeBehavior` для усыпления.

#### Поведения врагов (ZombieBehavior, VampireBehavior, GhostBehavior, OgreBehavior, SnakeBehavior, MimicBehavior)
**Специфичное поведение врагов.** Каждый переопределяет `Tick()`:
- **ZombieBehavior**: стандартное поведение - движение к игроку при активации, случайное движение иначе, атака на расстоянии 1.
- **VampireBehavior**: переопределяет `TakeDamage()` - первая атака всегда промахивается (`EvadedFirstAttack`), остальное как у Zombie.
- **GhostBehavior**: при активации `IsInvisible = false`. Метод `Teleport()` телепортирует в пределах текущей комнаты (`TeleportInRoom()`) или коридора (`TeleportInCorridor()`), используя `Map.FindRoomAt()` и `Map.FindCorridorAt()`.
- **OgreBehavior**: переопределяет `TakeDamage()` (простая обертка). Метод `MoveTwice()` пытается переместиться на 2 клетки к игроку. Атака происходит через `Attack()` из базового класса.
- **SnakeBehavior**: переопределяет `SpecialEffectOnAttack()` - с шансом `Snake.SleepChancePercent` усыпляет игрока через `ActiveEffectManager.AddActiveEffect(EffectTypeEnum.Sleep)`. Метод `ChangeDirection()` меняет направление случайного движения.
- **MimicBehavior**: при активации меняет `Representation` с предмета на `Mimic`, остальное как у Zombie.

---

### 📁 Items/ — Предметы

#### `Item.cs`
**Базовый класс предмета.** Хранит `Position`, `Type` (Weapon/Food/Elixir/Scroll/Treasure). Базовый класс для всех предметов.

#### `Weapon.cs`
**Оружие.** Наследуется от `Item`. Имеет `WeaponType` (Sword/Bow/Staff) и `StrengthBonus` (бонус к силе при экипировке).

#### `Food.cs`
**Еда.** Наследуется от `Item`, реализует `IUsableItem`. Имеет `HealthValue` (5-20 HP, задается при создании). Метод `Use()` восстанавливает HP через `hero.Heal()`, но не больше `MaxHp`.

#### `Elixir.cs`
**Эликсир.** Наследуется от `Item`, реализует `IUsableItem`. Имеет `ElixirType` (BuffStrength/BuffAgility/BuffMaxHp). Метод `Use()` создает `ActiveEffect` с случайным значением (1 до 10% от базовой характеристики) и длительностью 30-60 тиков, добавляет через `ActiveEffectManager.AddActiveEffect()`.

#### `Scroll.cs`
**Свиток.** Наследуется от `Item`, реализует `IUsableItem`. Имеет `ScrollType` (Strength/Agility/MaxHp). Метод `Use()` напрямую изменяет базовые характеристики: `ChangeBaseAgility(5)`, `ChangeBaseStrength(5)`, или `ChangeBaseMaxHp(10)` + `Heal(10)` для MaxHp.

#### `Treasure.cs`
**Сокровище.** Наследуется от `Item`. Имеет `Price` (стоимость в золоте) и `Level` (уровень, на котором было получено). При подборе несколько сокровищ объединяются в одно.

#### `Interfaces/IUsableItem.cs`
**Интерфейс расходуемых предметов.** Определяет метод `Use(Hero hero)` для использования предмета.

---

### 📁 Effects/ — Эффекты и усиления

#### `ActiveEffect.cs`
**Активный эффект.** Представляет временный эффект. Хранит `Type` (BuffStrength/BuffAgility/BuffMaxHp/Sleep), `Value` (значение эффекта), `RemainingTicks` (оставшиеся тики), `IsEffectOver` (флаг завершения). Конструктор без параметров создает эффект с дефолтными значениями (Sleep: 1 тик, баффы: 5 тиков, значения: 5/5/10). Конструктор с параметрами используется для эликсиров с кастомными значениями и длительностью. Метод `Tick()` уменьшает `RemainingTicks` и устанавливает `IsEffectOver` при достижении 0.

#### `ActiveEffectManager.cs`
**Менеджер эффектов.** Управляет списком активных эффектов (`ActiveEffects`) на персонаже. Методы: `AddActiveEffect()` (добавляет эффект, применяет состояние через `ApplyStateEffect()` - для Sleep вызывает `Hero.ApplySleep()`), `TickEffects()` (уменьшает тики всех эффектов через `effect.Tick()`, удаляет истекшие через `RemoveStateEffect()` - для Sleep вызывает `Hero.WakeUp()`, для BuffMaxHp лечит на 1 HP если HP <= 0), `GetTotalStatBonus()` (суммирует значения всех эффектов указанного типа через `ApplyStatEffect()`).

#### `WeaponManager.cs`
**Менеджер оружия.** Управляет экипированным оружием игрока. Хранит `EquippedWeapon` (может быть null). Методы: `EquipWeapon()` (сохраняет предыдущее оружие, заменяет на новое, возвращает предыдущее), `UnequipWeapon()` (снимает оружие, возвращает его), `GetStrengthBonus()` (возвращает `StrengthBonus` экипированного оружия или 0).

---

### 📁 Корневые файлы domain/

#### `PlayerAction.cs`
**Действие игрока.** Класс-обертка для действий игрока. Содержит `Type` (Move/UseItem/EquipWeapon/etc.), `TargetX/TargetY` (для движения), `SelectedItem` (для действий с предметами). Создается через статические методы `CreateMove()`, `CreateUseItem()` и т.д.

#### `Backpack.cs`
**Рюкзак игрока.** Управляет инвентарем. Хранит предметы по типам в словаре. Ограничение: максимум 9 предметов одного типа (кроме сокровищ, которые объединяются). Методы `Add()`, `DeleteItem()`, `UseItem()`, `EquipWeapon()`.

#### `GameObject.cs`
**Базовый класс игрового объекта.** Хранит `Position`. Базовый класс для всех сущностей на карте (персонажи, предметы).

#### `IGameObject.cs`
**Интерфейс игрового объекта.** Определяет свойство `Position` для всех объектов на карте.

---


- **ZombieBehavior**: стандартное поведение - движение к игроку при активации, случайное движение иначе, атака на расстоянии 1.
- **VampireBehavior**: переопределяет `TakeDamage()` - первая атака всегда промахивается (`EvadedFirstAttack`), остальное как у Zombie.
- **GhostBehavior**: при активации `IsInvisible = false`. Метод `Teleport()` телепортирует в пределах текущей комнаты (`TeleportInRoom()`) или коридора (`TeleportInCorridor()`), используя `Map.FindRoomAt()` и `Map.FindCorridorAt()`.
- **OgreBehavior**: переопределяет `TakeDamage()` (простая обертка). Метод `MoveTwice()` пытается переместиться на 2 клетки к игроку. Атака происходит через `Attack()` из базового класса.
- **SnakeBehavior**: переопределяет `SpecialEffectOnAttack()` - с шансом `Snake.SleepChancePercent` усыпляет игрока через `ActiveEffectManager.AddActiveEffect(EffectTypeEnum.Sleep)`. Метод `ChangeDirection()` меняет направление случайного движения.
- **MimicBehavior**: при активации меняет `Representation` с предмета на `Mimic`, остальное как у Zombie.

---

### 📁 Items/ — Предметы

#### `Item.cs`
**Базовый класс предмета.** Хранит `Position`, `Type` (Weapon/Food/Elixir/Scroll/Treasure). Базовый класс для всех предметов.

#### `Weapon.cs`
**Оружие.** Наследуется от `Item`. Имеет `WeaponType` (Sword/Bow/Staff) и `StrengthBonus` (бонус к силе при экипировке).

#### `Food.cs`
**Еда.** Наследуется от `Item`, реализует `IUsableItem`. Имеет `HealthValue` (5-20 HP, задается при создании). Метод `Use()` восстанавливает HP через `hero.Heal()`, но не больше `MaxHp`.

#### `Elixir.cs`
**Эликсир.** Наследуется от `Item`, реализует `IUsableItem`. Имеет `ElixirType` (BuffStrength/BuffAgility/BuffMaxHp). Метод `Use()` создает `ActiveEffect` с случайным значением (1 до 10% от базовой характеристики) и длительностью 30-60 тиков, добавляет через `ActiveEffectManager.AddActiveEffect()`.

#### `Scroll.cs`
**Свиток.** Наследуется от `Item`, реализует `IUsableItem`. Имеет `ScrollType` (Strength/Agility/MaxHp). Метод `Use()` напрямую изменяет базовые характеристики: `ChangeBaseAgility(5)`, `ChangeBaseStrength(5)`, или `ChangeBaseMaxHp(10)` + `Heal(10)` для MaxHp.

#### `Treasure.cs`
**Сокровище.** Наследуется от `Item`. Имеет `Price` (стоимость в золоте) и `Level` (уровень, на котором было получено). При подборе несколько сокровищ объединяются в одно.

#### `Interfaces/IUsableItem.cs`
**Интерфейс расходуемых предметов.** Определяет метод `Use(Hero hero)` для использования предмета.

---

### 📁 Effects/ — Эффекты и усиления

#### `ActiveEffect.cs`
**Активный эффект.** Представляет временный эффект. Хранит `Type` (BuffStrength/BuffAgility/BuffMaxHp/Sleep), `Value` (значение эффекта), `RemainingTicks` (оставшиеся тики), `IsEffectOver` (флаг завершения). Конструктор без параметров создает эффект с дефолтными значениями (Sleep: 1 тик, баффы: 5 тиков, значения: 5/5/10). Конструктор с параметрами используется для эликсиров с кастомными значениями и длительностью. Метод `Tick()` уменьшает `RemainingTicks` и устанавливает `IsEffectOver` при достижении 0.

#### `ActiveEffectManager.cs`
**Менеджер эффектов.** Управляет списком активных эффектов (`ActiveEffects`) на персонаже. Методы: `AddActiveEffect()` (добавляет эффект, применяет состояние через `ApplyStateEffect()` - для Sleep вызывает `Hero.ApplySleep()`), `TickEffects()` (уменьшает тики всех эффектов через `effect.Tick()`, удаляет истекшие через `RemoveStateEffect()` - для Sleep вызывает `Hero.WakeUp()`, для BuffMaxHp лечит на 1 HP если HP <= 0), `GetTotalStatBonus()` (суммирует значения всех эффектов указанного типа через `ApplyStatEffect()`).

#### `WeaponManager.cs`
**Менеджер оружия.** Управляет экипированным оружием игрока. Хранит `EquippedWeapon` (может быть null). Методы: `EquipWeapon()` (сохраняет предыдущее оружие, заменяет на новое, возвращает предыдущее), `UnequipWeapon()` (снимает оружие, возвращает его), `GetStrengthBonus()` (возвращает `StrengthBonus` экипированного оружия или 0).

---

### 📁 Корневые файлы domain/

#### `PlayerAction.cs`
**Действие игрока.** Класс-обертка для действий игрока. Содержит `Type` (Move/UseItem/EquipWeapon/etc.), `TargetX/TargetY` (для движения), `SelectedItem` (для действий с предметами). Создается через статические методы `CreateMove()`, `CreateUseItem()` и т.д.

#### `Backpack.cs`
**Рюкзак игрока.** Управляет инвентарем. Хранит предметы по типам в словаре. Ограничение: максимум 9 предметов одного типа (кроме сокровищ, которые объединяются). Методы `Add()`, `DeleteItem()`, `UseItem()`, `EquipWeapon()`.

#### `GameObject.cs`
**Базовый класс игрового объекта.** Хранит `Position`. Базовый класс для всех сущностей на карте (персонажи, предметы).

#### `IGameObject.cs`
**Интерфейс игрового объекта.** Определяет свойство `Position` для всех объектов на карте.

---


---

## DataLayer — Сохранение и загрузка игры

### 📁 Структура DataLayer

DataLayer отвечает за персистентность игровых данных: сохранение игры в JSON и загрузку обратно. Использует паттерн Mapper для преобразования между Domain-объектами и моделями сохранения.

### 📁 GameDataService.cs — Основной сервис

**Сервис для работы с сохранением и загрузкой.** [datalayer/GameDataService.cs]

**Основные методы:**

1. **`GameSave CreateSave(Hero, List<Enemy>, List<Item>, Level, GameStatistics)`** [datalayer/GameDataService.cs:17]
   - Создает объект `GameSave` из текущего состояния игры
   - Использует мапперы для преобразования Domain-объектов в Save-модели

2. **`void SaveToFile(GameSave, string filePath)`** [datalayer/GameDataService.cs:32]
   - Сериализует `GameSave` в JSON и сохраняет в файл
   - Использует `JsonSerializer` с форматированием (indented)

3. **`GameSession LoadGame(string filePath)`** [datalayer/GameDataService.cs:63]
   - Загружает игру из JSON файла
   - Восстанавливает все объекты через мапперы
   - **Восстанавливает номер уровня** из сохранения (`LevelMapper.FromSave()` восстанавливает `LevelNumber` [datalayer/Mappers/LevelMapper.cs:25])
   - Размещает сущности на уровне
   - Возвращает готовую `GameSession` с восстановленной статистикой и правильным номером уровня

4. **`void AddAttemptToScoreboard(GameStatistics, string scoreboardPath)`** [datalayer/GameDataService.cs:120]
   - Добавляет попытку прохождения в таблицу лидеров
   - Вызывается при завершении игры (смерть/победа)
   - Загружает существующую таблицу или создает новую

7. **`List<GameStatisticsSave> GetTopAttempts(string scoreboardPath, int count)`** [datalayer/GameDataService.cs:161]
   - Возвращает лучшие попытки, отсортированные по количеству сокровищ
   - Используется для отображения таблицы лидеров

**Поток сохранения игры:**
```
GameSession (Domain)
  ↓
GameDataService.CreateSave() [datalayer/GameDataService.cs:17]
  ├─ HeroMapper.ToSave() → HeroSave
  ├─ EnemyMapper.ToSave() → EnemySave (для каждого врага)
  ├─ ItemMapper.ToSave() → ItemSave (для каждого предмета)
  ├─ LevelMapper.ToSave() → LevelSave (включая LevelNumber)
  └─ GameStatisticsMapper.ToSave() → GameStatisticsSave
  ↓
GameSave (модель сохранения)
  ↓
GameDataService.SaveToFile() [datalayer/GameDataService.cs:32]
  ├─ JsonSerializer.Serialize() → JSON строка
  └─ File.WriteAllText() → сохранение в файл
```

**Поток загрузки игры:**
```
JSON файл
  ↓
GameDataService.LoadGame() [datalayer/GameDataService.cs:63]
  ├─ LoadFromFile() → GameSave
  ├─ LevelMapper.FromSave() → Level (восстанавливает LevelNumber из сохранения)
  ├─ HeroMapper.FromSave() → Hero
  ├─ EnemyMapper.FromSave() → List<Enemy>
  ├─ ItemMapper.FromSave() → List<Item>
  └─ GameStatisticsMapper.FromSave() → GameStatistics
  ↓
Размещение сущностей на уровне
  ↓
new GameSession(level, hero, level.LevelNumber, statistics)
  ↓
Готовая GameSession с правильным номером уровня
```
#### Автоматическое сохранение

**Полная игра сохраняется автоматически** в двух случаях (по ТЗ: "После прохождения каждого уровня" и "После перезапуска игры"):

- **При переходе на новый уровень** [presentation/GameController.cs:75-78]:
  ```csharp
  // Сохраняем полную игру при переходе на новый уровень
  if (_session.CurrentLevelNumber > levelBeforeTurn)
  {
      SaveFullGame(); // Сохраняет героя, врагов, предметы, уровень, статистику
  }
  ```

- **При выходе из игры (Quit)** [presentation/GameController.cs:82-86]:
  ```csharp
  // Сохраняем полную игру перед выходом
  if (action.Type == PlayerActionType.Quit)
  {
      SaveFullGame();
      _running = false;
  }
  ```

**Фронтенд не должен вызывать сохранение вручную** — это происходит автоматически.

#### Добавление в таблицу лидеров

**Добавление происходит автоматически** при завершении игры в `GameController.HandleInput()` [presentation/GameController.cs:80]:
```csharp
// Вызывается автоматически при смерти/победе
if (_session.IsGameOver() || _session.IsGameCompleted)
{
    GameDataService.AddAttemptToScoreboard(_session.Statistics, ScoreboardFilePath);
}
```
**Важно:** При загрузке игры номер уровня восстанавливается автоматически из сохранения. Не нужно передавать `levelNumber` в `GameInitializer.CreateNewGame()` — `GameDataService.LoadGame()` уже возвращает `GameSession` с правильным уровнем.

**Поток загрузки игры:**
```
JSON файл
  ↓
GameDataService.LoadGame() [datalayer/GameDataService.cs:63]
  ├─ File.ReadAllText() → чтение файла
  ├─ JsonSerializer.Deserialize() → GameSave
  ├─ HeroMapper.FromSave() → Hero
  ├─ EnemyMapper.FromSave() → Enemy (для каждого врага)
  ├─ ItemMapper.FromSave() → Item (для каждого предмета)
  ├─ LevelMapper.FromSave() → Level
  └─ GameStatisticsMapper.FromSave() → GameStatistics
  ↓
Размещение сущностей на уровне [datalayer/GameDataService.cs:75-83]
  ├─ level.AddEntity(hero)
  ├─ level.AddEntity(enemy) для каждого врага
  └─ level.AddEntity(item) для каждого предмета
  ↓
GameSession(level, hero, levelNumber, statistics) → возврат готовой сессии
```

### 📁 Models/ — Модели сохранения

Модели сохранения — простые DTO-классы для сериализации в JSON. Содержат только примитивные типы и списки.

#### `GameSave.cs`
**Главная модель сохранения.** Содержит все данные игры: `Hero`, `Enemies`, `Items`, `Level`, `Statistics`, `GameLevel`.

#### `HeroSave.cs`
**Модель сохранения героя.** Хранит позицию, характеристики (HP, сила, ловкость), активные эффекты, экипированное оружие, содержимое рюкзака, состояние сна.

#### `EnemySave.cs`
**Модель сохранения врага.** Хранит тип врага, позицию, HP, флаг активации. Специфичные свойства для разных типов: `OgreCooldown` (Ogre), `IsInvisible` (Ghost), `EvadedFirstAttack` (Vampire), `Representation` (Mimic).

#### `ItemSave.cs`
**Модель сохранения предмета.** Хранит тип предмета, позицию. Специфичные свойства: `HealthValue` (Food), `ElixirType` (Elixir), `ScrollType` (Scroll), `WeaponType` и `StrengthBonus` (Weapon), `Price` (Treasure), `GameLevel` (Treasure).

#### `LevelSave.cs`
**Модель сохранения уровня.** Хранит номер уровня, список комнат (`RoomSave`), список коридоров (`CorridorSave`), позиции старта и выхода.

#### `GameStatisticsSave.cs`
**Модель сохранения статистики.** Хранит все метрики: количество сокровищ, максимальный уровень, убитые враги, использованные предметы, попадания/промахи, полученный урон, пройденные клетки.

#### `ScoreboardSave.cs`
**Модель таблицы лидеров.** Содержит список всех попыток прохождения (`List<GameStatisticsSave>`).

#### `RoomSave.cs`, `CorridorSave.cs`, `PositionSave.cs`, `ActiveEffectSave.cs`
**Вспомогательные модели** для сохранения геометрии уровня и эффектов.

### 📁 Mappers/ — Преобразование Domain ↔ Save

Мапперы преобразуют Domain-объекты в модели сохранения и обратно.

#### `HeroMapper.cs`
**Маппинг героя.** Методы `ToSave(Hero)` и `FromSave(HeroSave)`. Сохраняет/восстанавливает позицию, характеристики, активные эффекты, экипированное оружие, содержимое рюкзака.

#### `EnemyMapper.cs`
**Маппинг врагов.** Методы `ToSave(Enemy)` и `FromSave(EnemySave)`. Обрабатывает специфичные свойства для каждого типа врага (Ogre, Ghost, Vampire, Mimic) через pattern matching.

#### `ItemMapper.cs`
**Маппинг предметов.** Методы `ToSave(Item)` и `FromSave(ItemSave)`. Обрабатывает разные типы предметов через switch, сохраняет специфичные свойства.

#### `LevelMapper.cs`
**Маппинг уровня.** Методы `ToSave(Level)` и `FromSave(LevelSave)`. Сохраняет/восстанавливает геометрию уровня (комнаты, коридоры, позиции).

#### `GameStatisticsMapper.cs`
**Маппинг статистики.** Методы `ToSave(GameStatistics)` и `FromSave(GameStatisticsSave)`. Преобразует все метрики статистики.

#### `RoomMapper.cs`, `CorridorMapper.cs`, `PositionMapper.cs`, `ActiveEffectMapper.cs`
**Вспомогательные мапперы** для геометрии уровня и эффектов.



**Поток инициализации:**

```
Frontend
  ↓
GameInitializer.CreateNewGame(levelNumber) [Session/GameInitializer.cs]
  ↓
LevelGenerator.GenerateLevel() [generation/LevelGenerator.cs] → создает Level (комнаты, коридоры)
  ↓
EntityGenerator.PlacePlayer() [generation/EntityGenerator.cs] → создает Hero и размещает на старте
  ↓
EntityGenerator.PlaceEnemies() [generation/EntityGenerator.cs] → создает и размещает врагов
  ↓
EntityGenerator.PlaceItems() [generation/EntityGenerator.cs] → создает и размещает предметы
  ↓
GameSession(level, hero, levelNumber) [Session/GameSession.cs] → создает игровую сессию
  ↓
GameController(gameSession) [presentation/GameController.cs] → создает контроллер
  ↓
controller.GetGameStateViewModel() [presentation/GameController.cs] → получает данные для отрисовки
```

### 2. Обработка хода игрока

```
Frontend → GameController.HandleInput(char key) [presentation/GameController.cs]
  ↓
GameController создает PlayerAction [domain/PlayerAction.cs] (Move/UseItem/EquipWeapon/etc.)
  ↓
GameController → GameSession.ProcessTurn(PlayerAction) [Session/GameSession.Turn.cs]
  ↓
GameSession.Turn.ProcessTurn() [Session/GameSession.Turn.cs]:
  ├─ Player.ActiveEffectManager.TickEffects() [Effects/ActiveEffectManager.cs] → обновление эффектов
  ├─ switch (action.Type):
  │   ├─ Move → HandlePlayerMove() [Session/GameSession.Turn.cs]
  │   │   ├─ Проверка IsWalkable() [generation/Level.cs]
  │   │   ├─ Если на клетке враг → ProcessCombat(enemy) [Session/GameSession.Combat.cs]
  │   │   │   ├─ BattleService.HitSuccess() [Combat/BattleService.cs] → проверка попадания игрока
  │   │   │   ├─ CalculatePlayerDamage() [Session/GameSession.Combat.cs] → расчет урона игрока
  │   │   │   ├─ enemy.TakeDamage() [Characters/Сharacter.cs] → урон врагу
  │   │   │   ├─ Если враг мертв → создание Treasure [Items/Treasure.cs], удаление врага [generation/Level.cs]
  │   │   │   ├─ BattleService.HitSuccess() [Combat/BattleService.cs] → проверка попадания врага
  │   │   │   ├─ CalculateEnemyDamage() [Session/GameSession.Combat.cs] → расчет урона врага
  │   │   │   ├─ Player.TakeDamage() [Characters/Сharacter.cs] → урон игроку
  │   │   │   └─ ApplyEnemySpecialOnHit() [Session/GameSession.Combat.cs] → спецэффекты (Snake сон, Ghost видимость)
  │   │   └─ Если враг убит → MovePlayer() [Session/GameSession.Movement.cs] → перемещение на клетку
  │   ├─ UseItem → UsePlayerItem() [Session/GameSession.Inventory.cs]
  │   ├─ EquipWeapon → EquipPlayerWeapon() [Session/GameSession.Inventory.cs]
  │   ├─ UnequipWeapon → UnequipPlayerWeapon() [Session/GameSession.Inventory.cs]
  │   └─ DropItem → DropPlayerItem() [Session/GameSession.Inventory.cs]
  ├─ MovePlayer() [Session/GameSession.Movement.cs] (если не было боя):
  │   ├─ MoveEntity() [Session/GameSession.Movement.cs] → перемещение
  │   └─ CheckForInteractions() [Session/GameSession.Movement.cs] → автоматический подбор предметов
  └─ ProcessEnemiesTurn() [Session/GameSession.Enemies.cs] → ход врагов
      ├─ Для каждого врага: CreateEnemyBehavior() [Session/GameSession.Enemies.cs] → создание поведения
      └─ behavior.Tick(Player) [Characters/Behavior/EnemyBehavior.cs и наследники]:
          ├─ DistanceToHero() [Characters/Behavior/EnemyBehavior.cs] → расчет расстояния
          ├─ Если в радиусе HostilityLevel:
          │   ├─ Enemy.IsTriggered = true [Characters/Enemy.cs]
          │   ├─ MoveTowards() / MoveRandom() [Characters/Behavior/EnemyBehavior.cs] → движение к игроку
          │   └─ Если расстояние = 1 → Attack(hero) [Characters/Behavior/CharacterBehavior.cs]
          │       ├─ BattleService.HitSuccess() [Combat/BattleService.cs] → проверка попадания
          │       └─ hero.TakeDamage() [Characters/Сharacter.cs] → урон игроку напрямую
          └─ SpecialEffectOnAttack() [Characters/Behavior/SnakeBehavior.cs] → спецэффекты (только Snake)
  ↓
Если игрок на выходе → AdvanceToNextLevelInternal() [Session/GameSession.Leveling.cs]
  ├─ LevelGenerator.GenerateLevel() [generation/LevelGenerator.cs] → новый уровень
  ├─ EntityGenerator.PlaceExistingHero() [generation/EntityGenerator.cs] → перемещение героя
  ├─ EntityGenerator.PlaceEnemies() [generation/EntityGenerator.cs] → размещение врагов
  └─ EntityGenerator.PlaceItems() [generation/EntityGenerator.cs] → размещение предметов
```

### 3. Получение состояния для отрисовки

```
Frontend → GameController.GetGameStateViewModel() [presentation/GameController.cs]
  ↓
GameSession.GetGameState() [Session/GameSession.State.cs] → создает GameState [Session/GameState.cs]
  ├─ PlayerPosition, PlayerHealth, PlayerMaxHealth [Characters/Hero.cs]
  ├─ PlayerAgility, PlayerStrength, PlayerIsSleep [Characters/Hero.cs]
  ├─ Enemies (список всех врагов) [generation/Level.cs]
  ├─ Items (список всех предметов) [generation/Level.cs]
  ├─ LevelGeometry (Level с комнатами/коридорами) [generation/Level.cs]
  └─ CurrentLevelNumber [Session/GameSession.cs]
  ↓
GameStateMapper.ToViewModel(GameState) [presentation/Mappers/GameStateMapper.cs]
  ├─ ToPlayerViewModel() [presentation/Mappers/GameStateMapper.cs] → PlayerViewModel
  ├─ ToEnemyViewModel() [presentation/Mappers/GameStateMapper.cs] → EnemyViewModel (для каждого врага)
  ├─ ToItemViewModel() [presentation/Mappers/GameStateMapper.cs] → ItemViewModel (для каждого предмета)
  └─ ToLevelViewModel() [presentation/Mappers/GameStateMapper.cs] → LevelViewModel
  ↓
GameStateViewModel → возвращается во Frontend
```
